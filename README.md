## Deploying terraform to azure

Initiazile terraform in command line.

```cmd
terraform init
```

Check resources to be deployed.

```cmd
terraform plan
```

Deploy resources to azure

```cmd
terraform apply
```

